node_modules
dist
.DS_Store
*.local
.vite
_shots
tmp/

# Ad-hoc verification scripts (prefixed _) stay local.
scripts/_*.mjs

# Measurement stand-ins and other non-canonical working files.
.netora-work/

# Baseline checkpoint screenshots. The harness writes them on capture and they
# are worth looking at when a number moves, but nothing verifies against them —
# `state*.json` is what the comparison reads — so they are not carried in the
# repository.
baselines/act-one/frames/
baselines/act-one/frames-*/
